[package]
name = "winter-fri"
version = "0.12.0"
description = "Implementation of FRI protocol for the Winterfell STARK prover/verifier"
authors = ["winterfell contributors"]
readme = "README.md"
license = "MIT"
repository = "https://github.com/novifinancial/winterfell"
documentation = "https://docs.rs/winter-fri/0.12.0"
categories = ["cryptography", "no-std"]
keywords = ["crypto", "polynomial", "commitments"]
edition = "2021"
rust-version = "1.84"

[lib]
bench = false

[[bench]]
name = "prover"
harness = false

[[bench]]
name = "folding"
harness = false

[[bench]]
name = "fold_and_batch_folding"
harness = false

[[bench]]
name = "fold_and_batch_master"
harness = false

[[bench]]
name = "fold_and_batch_verify"
harness = false

[[bench]]
name = "parallel_fri_prover"
harness = false

[[bench]]
name = "parallel_fri_verify"
harness = false

[[example]]
name = "frittata_bench"

[features]
concurrent = ["crypto/concurrent", "math/concurrent", "utils/concurrent", "std"]
default = ["std"]
std = ["crypto/std", "math/std", "utils/std"]

[dependencies]
crypto = { version = "0.12", path = "../crypto", package = "winter-crypto", default-features = false }
math = { version = "0.12", path = "../math", package = "winter-math", default-features = false }
utils = { version = "0.12", path = "../utils/core", package = "winter-utils", default-features = false }
rand-utils = { version = "0.12", path = "../utils/rand", package = "winter-rand-utils" }

[dev-dependencies]
criterion = "0.5"
rand-utils = { version = "0.12", path = "../utils/rand", package = "winter-rand-utils" }
